export interface HarmonyConfig {
  maxBonus: number;
  weakestFloor: number;
  spreadCeiling: number;
}

export const HARMONY_CONFIG: HarmonyConfig = {
  maxBonus: 3,
  weakestFloor: 40,
  spreadCeiling: 50,
};
